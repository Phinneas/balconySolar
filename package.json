{
  "name": "balconysolar",
  "version": "1.0.0",
  "description": "",
  "main": "populate-teable.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "npm install --legacy-peer-deps && npm run build:frontend && npm run build:api && npm run build:scraper",
    "build:frontend": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "build:api": "cd api && npm install && npm run build",
    "build:scraper": "cd scraper && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "fast-check": "^4.4.0",
    "jest": "^30.2.0"
  }
}
